# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Generated by `pnpm test:coverage`. Prettier and ESLint already ignored it;
# git did not, and a 70-file HTML report landed in a commit because of that.
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# This project uses pnpm. A stray `npm install` writes this file and gives the
# working tree a second, disagreeing dependency graph — never commit it.
package-lock.json
yarn.lock
