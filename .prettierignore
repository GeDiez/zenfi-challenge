dist
coverage
node_modules
pnpm-lock.yaml

# Skills vendored from other repos: authored content, not this project's source.
# Reformatting them would rewrite someone else's document.
.claude/skills

# Raw exports are kept exactly as received, so a new drop diffs cleanly against
# the previous one instead of against Prettier's reformatting.
src/data/*.json
